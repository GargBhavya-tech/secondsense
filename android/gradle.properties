org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# QAIRT/QNN SDK (Qualcomm AI Engine Direct) — downloaded via QPM, contains include/QNN and
# lib/aarch64-android that the gated native build (build.gradle.kts, -PenableQnnNative=true)
# needs. CMake reads this as -DQNN_SDK_ROOT.
qnn.sdk.root=C\:/Master_Brain/Projects/qairt/2.49.0.260730
